# Excluded from the `.mcpb` package. The `uv` runtime resolves dependencies
# from the bundled pyproject.toml, so the virtual environment must NOT be shipped.
.git/
.github/
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
.eggs/
tests/
tools/
docs/
site/
.superpowers/
.pre-commit-config.yaml
*.mcpb
# Local credentials — never into the package (review: W8 patched
# `.gitignore`, but not this separate list).
.env*
