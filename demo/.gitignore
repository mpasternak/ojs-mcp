# Generated by setup.sh (downloaded template + a freshly generated
# api_key_secret and salt) and then rewritten by the OJS installer, which
# stores the database password in it. Nothing here is worth committing.
config/ojs.config.inc.php
