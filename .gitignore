# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# venv / uv
.venv/
venv/
.python-version

# dokumentacja (MkDocs build)
site/

# testy / narzędzia
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.mypy_cache/

# IDE / OS
.idea/
.vscode/
.DS_Store

# Poświadczenia lokalne (repozytorium jest publiczne; projekt konfiguruje
# się wyłącznie zmiennymi środowiskowymi — patrz docs/instalacja.md).
# `.env*` (nie goły `.env`), żeby złapać też `.env.local`/`.env.production`.
.env*

# Bundle MCPB zbudowany lokalnie (patrz docs/instalacja.md, wariant 2).
*.mcpb
